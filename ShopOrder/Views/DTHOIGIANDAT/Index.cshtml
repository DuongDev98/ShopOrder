@model IEnumerable<ShopOrder.Models.DTHOIGIANDAT>

@{
    ViewBag.Title = "Thời gian đặt";
    Layout = "~/Views/Shared/_Admin.cshtml";
}
<div class="row">
    <div class="col-lg-12">
        <h4 class="page-header">Danh sách thời gian đặt</h4>
    </div>
</div>
<p>
    @Html.ActionLink("Thêm mới", "Edit", null, new { @class = "btn btn-info" })
</p>
<table class="table">
    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NAME)
            </td>
            <td>
                @Html.ActionLink("Chỉnh sửa", "Edit", new { ID = item.ID }, new { @class = "btn btn-warning" })
                @Html.ActionLink("Xóa", "Delete", new { ID = item.ID }, new { @class = "btn btn-danger" })
            </td>
        </tr>
    }
</table>